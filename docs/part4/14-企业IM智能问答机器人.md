# 14 案例一：企业 IM 智能问答机器人——从 Dify 工作流到自建 MCP Server 架构

> 本章为 Part 4 付费内容，购买后可看到完整版。这里展示前半部分作为试读。

## 背景：一个真实的业务需求

我所在的团队每天要处理大量游戏数据报表查询——"这个游戏本周举报量多少""某个策略上线后效果怎么样"，这类问题原本要人工打开 BI 后台一个个查报表、拉数据、画图。我想做一个企业 IM 群机器人，@它就能自动查数据、给出分析。

这不是一个"要不要用 Agent"的问题——按Part 1 第04章的分级框架，这个需求路径明确不固定（用户可能问任意游戏、任意时间段、任意数据类型），至少需要 L1 单轮工具调用起步，实际业务复杂度决定了它需要走到"意图分类→参数提取→数据查询→分析生成"的多步流程。

## 第一版：用 Dify 工作流验证 MVP

按 Part 2 第06章的建议，第一步永远是用低代码平台验证业务逻辑可行性。第一版 Dify 工作流结构：

```
开始 → LLM(问题分类+提取游戏/时间/数据类型) → 代码执行(智能匹配报表+图表+计算日期) → 条件分支
  → IF(数据类查询) → HTTP请求(调数据平台API) → 数据汇总 → LLM(数据分析) → 直接回复
  → ELSE(知识类查询) → 知识检索 → LLM(知识问答) → 直接回复
```

半天时间跑通，验证了核心假设：**LLM 分类问题类型+提取结构化参数+调用真实数据接口，这条链路是可行的。** 这个阶段最大的收获不是代码，是确认了"这事能做"。

## 第一版暴露的三个真实问题

...

---

###🔒 完整版内容包含（购买后解锁）

- 从 Dify 迁移到企业微信自带工作流的完整过程，及为什么这一步也不是终态
- **真实踩过的核心 Bug**：企微HTTP 请求节点发送 POST body 后，Flask `get_json()` 返回 `None`——完整的根因排查过程（Content-Type 携带额外参数导致 Flask 严格匹配失效）和最终的 **7 层兜底解析策略**完整代码
- 为什么最终决定自建"MCP Server + REST API 共享业务逻辑层"架构，而不是继续在低代码平台上修修补补——附完整架构图
- 图片生成"非致命化"设计：一个关键的容错原则，如何避免非核心功能拖垮核心链路
- 完整的部署与验证记录：多个真实游戏项目的报表查询验证结果
- 这个案例对应Part 2 第06/07章、Part 3 第11章的**真实验证**，告诉你书里讲的框架在真实项目里是怎么一步步被用上的，以及框架之外还会遇到哪些没写在教科书里的坑

[解锁方式见仓库根目录 README](../../README.md#进阶版真实项目复盘怎么解锁)
